<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Task form Todo list</title>
    <link rel="stylesheet" href="style.css" />
    <style>
      .heading {
        font-size: 2rem;
        text-align: center;
        background-color: aquamarine;
        padding: 20px;
        border-bottom: 2px solid black;
      }
      .blue-btn {
        text-align: center;
        color: blue;
        padding: 8px 20px;
        margin: 2px;
        border: none;
        border-radius: 10px;
        border: 2px solid transparent;
      }

      .blue-btn:hover {
        box-shadow: 2px 2px 10px #11c3f0;
        border: 2px solid white;
        background-color: #50b0e7;
        color: white;
      }
      .flex-d {
        display: flex;
        flex-direction: column;
        justify-content: center;
        width: 25%;
        border: 2px solid black;
        margin: 0 auto;
        border-radius: 5px;
        background-color: #6fecbc;
      }
      .flex-btn {
        display: flex;
        flex-direction: column;
        margin: 7px;
      }
      .input {
        padding: 15px 10px;
        width: 85%;
        margin: 20px 10px;
        font-size: 1.4rem;
        font-weight: 300;
      }
      .buttons {
        display: flex;
        justify-content: space-around;
        align-items: center;
      }
      .buttons p {
        font-weight: bold;
        font-size: small;
        background-color: rgb(238, 253, 248);
        border-radius: 5px;
        margin: 10px;
        padding: 3px;
      }
    </style>
  </head>
  <body>
    <div class="form">
      <h2 class="heading">Editing Todos</h2>
      <div class="flex-d">
        <form action="/todo/<%=  Todos._id %>?_method=PUT" method="post">
          <textarea name="newTask" cols="30" class="input">
<%= Todos.task.trim() %></textarea
          >
          <div class="flex-btn">
            <button class="blue-btn">Update</button>
          </div>
        </form>

        <form action="/todo" method="get" class="flex-btn">
          <button class="blue-btn">Cancel</button>
        </form>
        <div class="buttons">
          <p><%=Todos.created_at.toString().split(" ")[4] %></p>
          <p>
            <%= Todos.created_at.toString().split(" ").slice(0,4).join(" ") %>
          </p>
        </div>
      </div>
    </div>
  </body>
</html>
